---
  - name: Shell Examples
    hosts: all
    tasks:
    - name: Display the file
      shell: "echo $HOSTNAME >> /tmp/yumlist.txt; yum list installed;echo "-----------------------------------------" >> /tmp/yumlist.txt"
      register: displaylist
      
    - debug: msg="{{displaylist.stdout_lines}}"
