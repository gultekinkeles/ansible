---
  - name: Yum list installed packages
    hosts: all
    tasks:
    - name: yum list installed packages and save to txt
      shell: python39 car.py
      register: deneme
    - debug: msg="{{deneme.stdout_lines}}"
